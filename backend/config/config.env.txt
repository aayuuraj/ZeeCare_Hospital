# Server Configuration
PORT=4000

# ============================================
# MySQL Configuration
# ============================================
# Option 1: Individual variables (for local development)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=hospital_management

# Option 2: Connection URL (for production — PlanetScale / Railway)
# If DATABASE_URL is set, it takes priority over individual variables.
# DATABASE_URL=mysql://user:password@host:port/database

# ============================================
# JWT Configuration
# ============================================
JWT_SECRET_KEY=abcd
JWT_EXPIRES=7d
COOKIE_EXPIRE=7

# ============================================
# Frontend URLs
# ============================================
FRONTEND_URL=http://localhost:5173
DASHBOARD_URL=http://localhost:5174

# ============================================
# Cloudinary Configuration (for image uploads)
# ============================================
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret